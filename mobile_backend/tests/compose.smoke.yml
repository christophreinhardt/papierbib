# Isolated local verification; contains only a public test password.
services:
  papierbib-api:
    container_name: papierbib-phase1-test
    ports: !override
      - "127.0.0.1:18888:8080"
    environment:
      PAPIERBIB_PASSWORD: only-for-isolated-container-test
      PUBLIC_ORIGIN: https://papierbib.test
